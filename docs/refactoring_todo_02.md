# リファクタリング計画

## 目的

コードベースの関心の分離を改善し、状態管理をより堅牢にすることで、将来の機能追加やメンテナンスを容易にする。

## TODOリスト

- [x] **[済] 現状分析**: コードベースを詳細に調査し、モジュール間の依存関係とデータフローを完全に把握する。
- [x] **[済] 状態管理の強化**: `state.js`を中央集権的なストアとして再設計し、状態の更新ロジックをカプセル化する。
- [x] **[済] UIと状態の分離**: UIコンポーネントが新しい状態ストアからデータを読み取り、Actionを発行して状態を更新するように変更する。
- [x] **UIモジュールの分割 (進行中)**
  - [x] `ui.js` からテーブル関連のロジックを `components/table.js` に分割。
  - [x] `ui.js` から参加者リスト関連のロジックを `components/participant.js` に分割。
  - [x] `ui.js` から動的セクション関連のロジックを `components/dynamicSection.js` に分割。
- [x] **計算ロジックの連携**: 新しい状態ストアのデータを元に計算を実行し、結果をストアに反映させる。
- [x] **テストの拡充**: 重要なビジネスロジックとUIコンポーネントのテストを追加・改善する。
- [x] **クリーンアップとドキュメント化**: 不要なコードを削除し、新しいアーキテクチャに合わせてドキュメントを更新する。
